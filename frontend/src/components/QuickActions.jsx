// QuickActions — standalone version kept for backward compat
// The Dashboard now renders its own inline quick-action grid with navigation.
// This file is retained in case other pages import it, but Dashboard.jsx
// now handles its own quick actions directly.
export default function QuickActions() {
  return null;
}
